services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    environment:
      - TZ=Asia/Seoul
    volumes:
      - ollama_data:/root/.ollama
    command: ["serve"]
    restart: unless-stopped

volumes:
  ollama_data:
    driver: local
